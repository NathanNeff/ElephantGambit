import requests
import time
import json

downloads = 100
wait_seconds = 60
imported_games = [:]
game_list_url = 'http://en.lichess.org/api/game?nb=200'

def get_new_games():
    json_data = open('one_line.json')
    data = json.load(json_data)
print req.content
time.sleep(wait_seconds);

data = [ ]

for game in data["list"]:
print game["id"]
print "----------------------------------------------------------------------"

~                      
